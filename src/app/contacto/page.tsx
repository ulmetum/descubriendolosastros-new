import { MultiStep } from '@/components/contactpage'

const ContactPage = () => {
  return (
    <div className='mt-[calc(var(--main-header-height))] min-h-[100dvh] flex-col flex justify-center w-full'>
      <MultiStep />
    </div>
  )
}
export default ContactPage
